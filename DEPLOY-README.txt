STRATUP SEO PRODUCTION V2

IMPORTANT: This package replaces hash routing with real clean URLs.
Copy ALL files/folders into the root of StratUp-Web, replacing index.html.

Then:
  git add .
  git commit -m "Migrate StratUp to SEO clean URLs and structured data"
  git push origin main

After Vercel deploy, test:
  /
  /capabilities
  /capabilities/growth-architecture
  /industries/real-estate
  /insights
  /resources/revenue-leakage-calculator
  /en/
  /en/capabilities
  /robots.txt
  /sitemap.xml

Do NOT submit Search Console sitemap until these URLs return 200 and navigation is verified.
